#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 *_strlen - check string length
 *@s: string to check
 *
 *Return: string length integer
 */

int _strlen(char *s)
{
	return strlen(s);
}
