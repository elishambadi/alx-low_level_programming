#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 *_strcmp - copy strings
 *@s1: string to copy to
 *@s2: string to be copied
 *
 *Return: int 1 if difference, 0 otherwise
 */

int _strcmp(char *s1, char *s2)
{
	return (strcmp(s1, s2));
}
