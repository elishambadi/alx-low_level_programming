#include <stdio.h>

void _putchar(char c);

int add(int a, int b)
{
	int sum = a + b;
	return (sum);
	/**
	char one, two, three;

	if (sum < 1000 && sum > 100)
	{
		one = (char)sum / 100;
		two = (char)sum / 10;
		three = (char)sum % 10;
		_putchar(one);
		_putchar(two);
		_putchar(three);
	}
	else if (sum < 100 && sum >= 10)
	{
		two =(char) sum / 10;
		three =(char) sum % 10;
		_putchar(two);
		_putchar(three);
	}
	else if (sum < 10)
	{
		three = (char) sum;
		_putchar(three);
	} 
	*/
}
